/* Hi there */

let
  type point = { x: int, y: int }
  var x : int := 2
  function f(z: int, s: string): int =
    z + size(s) - x
  var p:= point { x = 2, y = 8 }   /* Type inference!! */
in
  /* It’s a Tiger program */
  for i := 0 to x do
    print(concat(chr(f(p.y, "hello")), "\u{1f937}\n"))
end
